name: Release api

on:
  push:
    branches:
      - main
    paths:
      - 'api/**'

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    uses: ./.github/workflows/_release.yml
    with:
      path: api
    secrets: inherit
