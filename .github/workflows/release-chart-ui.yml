name: Release chart-ui

on:
  push:
    branches:
      - main
    paths:
      - 'charts/ui/**'

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    uses: ./.github/workflows/_release.yml
    with:
      path: charts/ui
    secrets: inherit
