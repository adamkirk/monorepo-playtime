name: Release cli

on:
  push:
    branches:
      - main
    paths:
      - 'cli/**'

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    uses: ./.github/workflows/_release.yml
    with:
      path: cli
    secrets: inherit
